# from django.urls import path, include
# from rest_framework.routers import DefaultRouter
# from .views import FarmerPaymentViewSet, CooperativePaymentViewSet
# router = DefaultRouter()
# router.register(r'farmer_payments', FarmerPaymentViewSet, basename='farmer_payments')
# router.register(r'cooperative_payments', CooperativePaymentViewSet, basename='cooperative_payments')
# urlpatterns = [
#     path('', include(router.urls)),
# ]